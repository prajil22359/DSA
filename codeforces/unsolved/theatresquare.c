#include <stdio.h>
#include <stdlib.h>
int main()
{
    int l, b, a;
    scanf("%d %d %d", &l, &b, &a);
    while (1>0)
    {
        
    }
    








    l = l - a;

    b = b - a;

    return 0;
}